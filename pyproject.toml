[project]
name = "keco-studio"
version = "0.1.0"
description = "Keco Studio - Game Design Tool"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "specify-cli @ git+https://github.com/github/spec-kit.git",
]
