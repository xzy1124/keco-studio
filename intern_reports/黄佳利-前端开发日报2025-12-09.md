黄佳利-前端开发日报

日期：2025 年 12 月 9 日

#### Task：

1. Research on how to use supabase db migrations (schema migration, schema init, deployment)；

2. Research on how to use figma MCP(May not need to use MCP) to achieve pixel level accurance；

3. Build the Login page；

4. Research on how to use spec-kit for spec driven development；

   

#### 工作内容：

#### **1.Supabase DB Migrations 研究与实践**

​       完成supabase db migrations相关流程研究与落地：

- 完成 Schema schema init：创建 `auth.users`（用户核心表）与 `public.profiles`（用户扩展信息表），通过外键 `id` 关联两张表，定义 `email`、`username`、`created_at` 等核心字段；
- 完成 Schema migration：按 Supabase 规范创建时间戳命名的迁移脚本（如 `20251209162746_update_handle_new_user.sql`），实现触发器函数的创建 / 更新，确保 `auth.users` 新增用户时自动同步数据到 `profiles` 表；

#### 2.Figma MCP 像素级还原研究与落地

​       基于 F2C MCP 工具探索 Figma 设计稿像素级还原方案：

- 配置 Figma 桌面版本地 MCP Server，完成 IDE 与 F2C MCP 工具的连接配置，实现设计稿结构化数据（图层尺寸、间距、样式等）的读取；

#### 3. 登录 / 注册页面开发

       完成登录、注册页面的组件化开发：

- 基于F2C MCP读取的设计稿数据，结合 Next.js + .module.css 实现登录 / 注册页面的像素级还原，覆盖布局、颜色、字体、交互态（输入框聚焦、按钮 hover 等）的复刻。
- 完成页面交互：实现登录 / 注册页的互相跳转、成功提示等交互效果。实现用户注册和登录功能，相关用户数据记录在auth.users和`profiles` 表中；

#### 4.Spec-kit 工具研究

       正在了解中